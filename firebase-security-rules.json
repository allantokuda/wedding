{
  "rules": {
    ".read": "auth != null && auth.uid == root.child('admin').val()",
    ".write": "auth != null && auth.uid == root.child('admin').val()",

    "invitation": {
      ".read": true,
      ".write": true
    }
  }
}
